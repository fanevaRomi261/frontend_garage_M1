import { EtatLibellePipe } from './etat-libelle.pipe';

describe('EtatLibellePipe', () => {
  it('create an instance', () => {
    const pipe = new EtatLibellePipe();
    expect(pipe).toBeTruthy();
  });
});
